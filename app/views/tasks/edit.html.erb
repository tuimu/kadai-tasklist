<h1>Edit Task id: <%= @task.id %></h1>

<%= form_with(model: @task, local: true) do |f| %>
    <%= f.label :todo, 'Task: ' %>
    <%= f.text_field :todo %>
    
    <%= f.submit 'Edit Task' %>
<% end %>

<%= link_to 'Back to Index', tasks_path %>