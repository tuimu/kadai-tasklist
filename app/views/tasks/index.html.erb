<h1>Tasks index</h1>

<ul>
    <% @tasks.each do |task| %>
    <li><%= link_to task.id, task_path(task) %>: &lt;<%= task.status %>&gt; <%= task.content %></li>
    <% end %>
</ul>

<%= link_to 'Create new Task', new_task_path %>